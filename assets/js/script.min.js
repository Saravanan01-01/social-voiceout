$(document).ready((function () {
    var n = 0;
    $(this).scrollTop(0), $(window).on("scroll load resize", (function () {
        var o = $(this).scrollTop();
        o > 200 ? $("#transmenu").removeClass("transparency") : $("#transmenu").addClass("transparency"), Math.abs(n - o) <= 5 || (o > 1200 && o > n ? $("#transmenu").fadeOut(600) : $("#transmenu").fadeIn(600), n = o)
    })), $("#transmenu .dropdown").on("show.bs.dropdown", (function () {
        $(this).find(".dropdown-menu").first().stop(!0, !0).slideDown(300)
    })), $("#transmenu .dropdown").on("hide.bs.dropdown", (function () {
        $(this).find(".dropdown-menu").first().stop(!0, !0).slideUp(300)
    }))
}));

$(document).ready((function () {
    $("#myCarousel").carousel({
        interval: 4e3
    });
    var a = !1;
    $("#myCarousel").on("click", ".nav a", (function () {
        a = !0, $(".nav li").removeClass("active"), $(this).parent().addClass("active")
    })).on("slid.bs.carousel", (function (e) {
        if (!a) {
            var i = $(".nav").children().length - 1,
                n = $(".nav li.active");
            n.removeClass("active").next().addClass("active"), i == parseInt(n.data("slide-to")) && $(".nav li").first().addClass("active")
        }
        a = !1
    }))
}));